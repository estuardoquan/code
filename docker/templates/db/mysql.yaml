services:
    mysql:
        image: mysql:latest
        environment:
            MYSQL_DATABASE: $DB_MYSQL_DATABASE
            MYSQL_ROOT_HOST: '%'
            MYSQL_ROOT_PASSWORD: $DB_MYSQL_PASSWORD

