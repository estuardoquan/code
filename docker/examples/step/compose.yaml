networks:
    local:
        driver: bridge
volumes:
    ca:
        driver: local
    crt_jwt:
        driver: local
secrets:
    provisioner_password:
        file: $STEP_PROVISIONER_PASSWORD_FILE
services:
    ca:
        extends:
            file: $TEMPLATE_STEP_CA
            service: ca
        volumes:
            - ca:$CA_STEPPATH
        networks:
            - local
        restart: unless-stopped
    crt:
        extends:
            file: $TEMPLATE_STEP_CRT
            service: crt
        volumes:
            - crt_jwt:/var/local/step
        networks:
            - local
        depends_on:
            - ca
        restart: unless-stopped

