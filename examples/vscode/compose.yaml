networks:
    local:
        driver: bridge
volumes:
    config:
        driver: local
services:
    app:
        extends:
            file: $TEMPLATE_VSCODE
            service: code_server
        volumes:
            - config:/config
        networks:
            - local 
        restart: unless-stopped

