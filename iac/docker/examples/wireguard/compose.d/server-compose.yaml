networks:
    local:
        driver: bridge
volumes:
    config:
        driver: local
services:
    vpn:
        extends:
            file: $TEMPLATE_WG_SERVER
            service: server
        volumes:
            - config:/config
        networks:
            - local
        restart: unless-stopped
